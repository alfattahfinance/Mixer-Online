/* Mixer Online — clean UI foundation.
   Kode tampilan mixer lama sengaja dihapus.
   Logic/UI baru akan dibangun dari fondasi ini. */
"use strict";
const state={channels:[],channelCount:0};
window.MixerOnline={state};
