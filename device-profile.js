/* Universal Device Profile & Mapping Engine
   Profiles describe capabilities; adapters later translate controls to real protocols. */
window.MixerProfiles=(()=>{const KEY="mixer-online-device-profile-v1";const defaults={name:"Universal / Custom",type:"custom",protocol:"none",channels:8,inputs:8,outputs:2,controls:["fader","gain","bass","mid","treble","pan","mute","solo"]};let profile=load()||defaults;let mappings={};function load(){try{return JSON.parse(localStorage.getItem(KEY)||"null")}catch{return null}}function save(){localStorage.setItem(KEY,JSON.stringify({profile,mappings}))}function configure(p){profile={...defaults,...profile,...p};save();return {...profile}}function setMapping(input,channel){mappings[String(input)]=Number(channel);save()}function getMapping(input){return mappings[String(input)]??null}function get(){return {profile:{...profile},mappings:{...mappings}}}return{configure,setMapping,get,getMapping,save}})();