/* Device feedback layer: translates incoming MIDI CC into normalized UI values. */
window.MixerFeedback=(()=>{function fromMidi(data,device){const status=data[0]&0xF0;if(status!==0xB0)return;const channel=data[0]&0x0F,cc=data[1],value=data[2];const maps=MixerProfiles.get().controlMappings||{};for(const [control,m] of Object.entries(maps)){if(Number(m.channel)===channel&&Number(m.cc)===cc){const lo=Number(m.min??0),hi=Number(m.max??1),v=lo+(value/127)*(hi-lo);window.dispatchEvent(new CustomEvent("mixer-feedback",{detail:{control,value:v,channel,cc,source:device?.name||"MIDI"}}));return}}}return{fromMidi}})();document.addEventListener("mixer-feedback",e=>{});